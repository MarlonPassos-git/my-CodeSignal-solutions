function avoidObstacles(inputArray) {
    let jumpSize = 2;
    
    increaseposition: while(true) {
        
        for (number of inputArray) {
            if (!(number % jumpSize)) {
                jumpSize++
                continue increaseposition
            }
        }
        return jumpSize   
    }
}
 
